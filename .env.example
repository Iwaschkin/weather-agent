# Copy to .env and fill in. .env is git-ignored; never commit real keys.
#
# Optional. Only the airspace tool (get_airspace / the drone assessment's
# airspace section) needs this; all weather, climate, air-quality, marine, and
# pollen tools work without any key. Get a key at https://www.openaip.net.
OPENAIP_API_KEY=
